[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live: tests that hit real AI APIs (cost money)
    slow: tests that take longer to run

# Default: run fast tests only
# Use -m live to include live API tests
addopts = -v --tb=short
